<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".sellerLogin">

    <ImageView
        android:id="@+id/seller_login_banner"
        android:layout_width="match_parent"
        android:layout_height="180dp"
        app:srcCompat="@drawable/sellerlogin" />

    <TextView
        android:id="@+id/sellerLogin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="20sp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_below="@+id/seller_login_banner"
        android:text="Seller Login" />

    <EditText
        android:id="@+id/S_loginEmail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Email"
        android:layout_marginTop="10dp"
        android:padding="15dp"
        android:layout_below="@+id/sellerLogin"
        ></EditText>

    <EditText
        android:id="@+id/S_loginPass"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Password"
        android:layout_marginTop="10dp"
        android:padding="15dp"
        android:layout_below="@+id/S_loginEmail"
        ></EditText>

    <Button
        android:id="@+id/S_login_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/buttons"
        android:layout_below="@id/S_loginPass"
        android:layout_marginTop="20dp"
        android:text="Login"

        ></Button>
</RelativeLayout>